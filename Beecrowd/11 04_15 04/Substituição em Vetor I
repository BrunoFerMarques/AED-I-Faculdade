#include <stdio.h>

int main(void) {
  int i, x[10];
  
  for(i = 0; i<10; i++){
    scanf("%d", &x[i]);
  }
  for(i = 0; i<10; i++){
    if(x[i] <= 0){
      x[i] = 1;
    }
  printf("X[%i] = %i\n", i, x[i]);
  }
  
  return 0;
}
